
export const enviarLista = (listaCompras) => {
    console.log(listaCompras)
    /* const endpoint = '/listasCompras'; // Cambia esto a la URL real del endpoint
    const options = {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify(listaCompras),
    };
  
    return fetch(endpoint, options)
      .then((response) => {
        if (!response.ok) {
          throw new Error('Error al enviar la lista de compras');
        }
        return response.json();
      }); */
  };
  
    